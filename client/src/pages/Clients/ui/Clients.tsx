import { Container } from '@/shared'

import { NewClient } from '@widgets/Client'

const ClientsPage = () => {
	return (
		<Container>
			<NewClient />
		</Container>
	)
}

export default ClientsPage
